# FinTrust AI — API Keys Configuration
# Copy this file to .env and fill in your keys.
# The app auto-detects whichever key is set (Perfios takes priority).

# ─── PERFIOS (Recommended — PAN + Bank Statement Analysis) ───────────────────
# Sign up: https://developer.perfios.com
# Dashboard: https://sandbox.api.perfios.com
PERFIOS_API_KEY=your_perfios_client_id_here
PERFIOS_SECRET=your_perfios_client_secret_here

# ─── SETU (PAN Verification only — free sandbox) ─────────────────────────────
# Sign up: https://bridge.setu.co → Sandbox → KYC → PAN Verification
SETU_CLIENT_ID=your_setu_client_id_here
SETU_CLIENT_SECRET=your_setu_client_secret_here

# ─── KARZA (PAN + Credit Enquiry) ────────────────────────────────────────────
# Sign up: https://karza.in
KARZA_API_KEY=your_karza_api_key_here

# ─── TransUnion CIBIL (Enterprise — requires RBI-registered entity) ───────────
# Contact: https://developer.transunion.com
CIBIL_API_KEY=your_cibil_bearer_token_here

# ─── Experian India (Enterprise) ─────────────────────────────────────────────
# Contact: https://www.experian.in
EXPERIAN_API_KEY=your_experian_bearer_token_here

# ─── NOTES ───────────────────────────────────────────────────────────────────
# If NO keys are set, the app automatically uses the built-in mock sandbox.
# The mock generates deterministic (same PAN = same score) realistic data.
# You can also set keys directly in the app's sidebar without any .env file.
